name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # 當 main 分支有更新時觸發

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # 因為我們的專案沒有傳統的 build 步驟，所以直接部署
      - name: Deploy 🚀
        uses: actions/deploy-pages@v4
